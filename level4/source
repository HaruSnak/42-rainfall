#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>

int m = 0;

void p(char *str) {
    printf(str);
}

void n(void) {
    char buffer[512];

    fgets(buffer, 512, stdin);
    p(buffer);
    if (m == 0x1025544)
    {
        system("/bin/sh");
    }
}

int main(void) {
    n();
    return 0;
}